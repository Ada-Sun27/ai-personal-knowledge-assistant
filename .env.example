# Default: no credentials, no paid model calls.
APP_PROFILE=demo
AWS_REGION=us-east-1
BEDROCK_CHAT_MODEL=amazon.nova-lite-v1:0
# For Bedrock, set APP_PROFILE=bedrock and use temporary AWS credentials.
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SESSION_TOKEN=
